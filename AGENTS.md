## 目录说明

博客文章创建在 docs/blog/posts 目录下，新建时，文件名以_（下划线）开头，意味着是草稿。草稿转正式发布需要由人类完成。

.runtime 用来写日志、生成调试、数据准备时需要的临时脚本

scripts 下存放与本项目运行、维护长期相关的脚本。

一般地，代码通过 fenced block 嵌入到markdown 文章中。但是，如果需要使用多个代码源文件，则允许把除入口之外，作为库文件存在的文件，放在与 markdown 文件同名的目录下。
